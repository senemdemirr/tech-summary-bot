Don’t Blink: Evidence Collapse during Multimodal Reasoning
Abstract
Reasoning VLMs can become more accurate while progressively losing visual grounding as they think. This creates task-conditional danger zones where low-entropy predictions are confident but ungrounded, a failure mode text-only monitoring cannot detect. Evaluating three reasoning VLMs on MathVista, HallusionBench, and MMMU_Pro, we find a pervasive evidence-collapse phenomenon: attention to annotated evidence regions drops substantially, often losing over half of evidence mass, as reasoning unfolds. Full-response entropy is the most reliable text-only uncertainty signal under cross-dataset transfer, yet adding vision features with a single global linear rule is brittle and often degrades transfer. An entropy-vision interaction model reveals a task-conditional regime: low-entropy, visually disengaged predictions are hazardous on sustained visual-reference tasks but benign on symbolic tasks. Using this structure, a targeted vision veto reduces selective risk by up to 1.9 percentage points at 90% coverage, while avoiding degradations where disengagement is expected. The results support task-aware multimodal monitoring for safe deployment under distribution shift. All code is publicly available at https://github.com/R-Suresh07/Dont-Blink.
1 Introduction
Uncertainty quantification is essential for the safe deployment of AI systems in high-stakes applications. Vision-Language Models (VLMs) trained via reinforcement learning to produce explicit reasoning traces, rather than relying on prompt-elicited chain-of-thought (hereafter RVLMs) achieve state-of-the-art accuracy on multimodal benchmarks spanning mathematical reasoning, expert-level knowledge, and visual question answering. Yet accuracy alone is insufficient for reliable decision-making: a model correct 80% of the time but reporting 95% confidence on every prediction is dangerous in domains where confident errors carry severe consequences. Calibration, the alignment of predicted confidence with true correctness probability, is a well-known challenge in modern deep neural networks [guo2017calibration], and evidence suggests it may worsen after reinforcement learning-based post-training.
Existing uncertainty methods for language models operate entirely over text token distributions: token entropy [xiong2024efficient], semantic clustering [kuhn2023semantic], prompt-based confidence elicitation [xiong2024can], and entropy discrimination that can emerge from RL post-training [sharma2025think]. Full-response entropy, averaged over the entire generation, can be a strong uncertainty monitor, but it is structurally incapable of distinguishing whether a confident prediction is grounded in visual evidence or has drifted into text-prior-driven confabulation. This motivates examining signals from the model’s own visual processing for failure modes that text-only monitoring cannot detect.
Work on visual evidence attribution shows that visual attention carries information beyond text signals [liu2025seeing], but prior methods target intervention at single generation steps rather than trajectory-level measurement across extended reasoning.
We make four contributions:
-
1.
Trajectory-level measurement of visual grounding in RVLMs. Evidence collapse is universal (9/9 cells) and often peaks mid-reasoning rather than at answer time.
-
2.
A strong transfer baseline for uncertainty monitoring. Full-response entropy outperforms alternatives under cross-dataset transfer, but cannot detect visual disengagement.
-
3.
Discovery of a task-conditional failure regime. An entropy–vision interaction model reveals a “confident-but-blind” penalty on sustained visual-reference tasks, absent or inverted on symbolic tasks.
-
4.
Task-aware monitoring design principles. A targeted vision veto validates the task-conditional structure: it helps where the interaction predicts danger and hurts where it predicts safety.
2 Related Work
Uncertainty estimation in language models. Modern deep neural networks are poorly calibrated despite high accuracy [guo2017calibration, naeini2015obtaining, nixon2019measuring]. Text-only uncertainty methods derive confidence from output distributions: token-level entropy and single-sample estimators [xiong2024efficient], semantic uncertainty via clustering sampled outputs under linguistic invariances [kuhn2023semantic], and prompt-based confidence elicitation [xiong2024can]; sequence-level entropy discrimination can emerge after RL post-training [sharma2025think]. These methods assume a unimodal text setting and do not account for visual evidence competing with accumulating text priors. Work on VLM uncertainty extends this line. chen2024unveiling and he2023investigating examine alignment and fine-tuning effects on calibration, while recent benchmarking reveals that VLM uncertainty scores are weak and model-dependent [wang2026vlmuqbench, lu2025prolonged, ortiz2026abstention]. Yet even these multimodal studies derive confidence from text outputs rather than modality-specific grounding dynamics. Across both lines, no method incorporates modality-specific grounding signals, and none tracks how such signals evolve across the generation trajectory.
Visual evidence attribution. Attention-based interpretability tools, from rollout methods to gradient-weighted attribution, aim to localize which image regions influence VLM outputs. liu2025seeing introduced evidence attribution for VLMs, showing that models can attend to correct evidence yet still err, but analyzed instruct-style generations at single endpoints and per-layer probes rather than reasoning trajectories. Recent intervention-focused work addresses grounding drift: jiang2026kvsmooth frame hallucination as semantic drift during decoding and propose KV-cache smoothing; adavboost2026 propose visual grounding entropy to estimate hallucination risk at individual generation steps. External verification approaches predict trust for spatial reasoning via object detection and geometric checks [imran2026trust], but operate outside the model’s internal representations. No prior work measures grounding trajectories across the full reasoning chain of RVLMs or connects trajectory dynamics to correctness under task-conditional variation.
3 Motivation
Entropy discrimination, where correct generations exhibit lower output entropy than incorrect ones, can emerge from RL post-training and is attractive for reasoning models because it requires no additional labels. This signal is strong but structurally compromised in the multimodal setting, motivating a modality-specific alternative.
RVLMs reason over fixed visual tokens: the image is encoded once and does not change as the reasoning chain grows token by token. As text accumulates, attention to the growing text context competes with attention to the fixed visual context, creating systematic pressure for visual attention to decay over the course of generation. Entropy operates entirely over text token distributions and cannot detect this pressure. A model answering a diagram-dependent question may generate a fluent, low-entropy response while having stopped attending to the diagram midway through reasoning: confident but visually disengaged. Whether this matters depends on whether the task requires sustained visual reference, and evidence collapse may not manifest only at the final answer but emerge progressively throughout the reasoning trajectory: the confident-but-blind failure mode that is the focus of this work.
If entropy is structurally blind to visual disengagement, we need signals from the visual modality itself. Visual attention to relevant image regions offers a white-box proxy, and cumulative grounding over the full reasoning trajectory may be more informative than any single-point snapshot, since the failure mode is progressive. The central question is whether the relationship between visual disengagement and error is consistent across task types, or whether visual disengagement is itself task-conditional.
4 Entropy-Attention Signals: Setup
4.1 Problem Setting
A vision-language model receives image and question , generating response token by token (length ). In our setting, RVLMs output a reasoning trace enclosed in <think>...</think> followed by a final answer; the </think> token marks the boundary between reasoning and answer spans. Our goal is to estimate using signals extractable during or after generation. Correctness is a binary label based on exact match or semantic equivalence with a ground-truth answer.
4.2 Textual Uncertainty Signal
Given top- log-probabilities at each generation step (), we compute normalized probabilities and Shannon entropy:
Token-averaged entropy across a span is . Because reasoning-chain dynamics and endpoint confidence may carry different signals, we distinguish two spans: Full-response entropy , where spans the entire generation (thinking + answer tokens), and Answer-span entropy , where spans answer tokens only.
4.3 Grounding Layer Selection
Following liu2025seeing, we compute per-layer AUROC on a held-out calibration set (100 Visual-CoT samples with bounding-box annotations), independent of evaluation datasets:
Here is the calibration set. For each sample , is a binary mask indicating which visual token positions fall inside the annotated bounding box, and is the vector of head-averaged attention weights to visual tokens at layer . The inner AUROC measures how well the layer’s attention discriminates evidence-region tokens from non-evidence visual tokens; averaging over gives the mean evidence localization ability of layer . We select the top- layers () as a contiguous block centred on the peak-AUROC layer. Letting , the architecture-specific grounding layer set is:
4.4 Visual Attention Mass
Let the input contain visual tokens; let denote their index set, so . At generation step , head of layer produces attention weights over all preceding tokens. Averaging over heads and aggregating over architecture-specific grounding layers , we define total visual attention mass:
Let denote tokens overlapping manually-annotated bounding boxes. Evidence attention mass is defined analogously, restricting the sum to evidence-region tokens:
Both and are absolute mass measures: a model that narrows focus to evidence regions while losing overall visual attention will show decreasing even if the fraction of visual attention on the bbox is unchanged.
5 Experiments
5.1 Experimental Setup
Datasets. We evaluate on three benchmarks: MathVista [lu2024mathvistaevaluatingmathematicalreasoning], which tests mathematical reasoning over geometric diagrams and algebraic visual expressions; HallusionBench [guan2024hallusionbenchadvanceddiagnosticsuite], which uses adversarially modified visual contexts designed to induce hallucinations; and MMMU_Pro [yue2025mmmuprorobustmultidisciplinemultimodal], which poses expert-level STEM questions spanning physics, chemistry, biology, and engineering. From each benchmark we draw a 300-sample stratified subset with manual bounding-box annotation, totaling 900 samples across 2,700 modelexample runs.
Models. We study three models across two architectural families. Qwen3-VL-2B-Thinking (hereafter Qwen-2B) and Qwen3-VL-8B-Thinking (hereafter Qwen-8B) [bai2025qwen3vl] employ the DeepStack architecture with multi-level visual feature injection across the transformer stack [meng2024deepstack]. GLM-4.6V-Flash (hereafter GLM) [glm2024glm46v] is a single-stream, token-level early-fusion VLM: a ViT-based visual encoder produces patch embeddings that are mapped into the language embedding space by an MLP adapter and injected as image tokens into a 40-layer decoder-only GLM text backbone for autoregressive generation.
Annotation. Bounding boxes were annotated by two annotators; guideline: box all image regions containing information necessary to answer correctly.
5.2 Text-Only Entropy Baseline
Before introducing visual grounding signals, we test whether text-only entropy provides reliable uncertainty estimation. We evaluate both entropy spans defined in §4: full-generation entropy () and answer-time entropy (), across all nine modeldataset cells, filtering for normal-stop responses.111Sample counts after filtering vary by model; full tables in Appendix B. We summarize discrimination strength per cell using Cohen’s (correct vs. incorrect): negative means incorrect answers have higher entropy than correct ones; positive means incorrect answers have lower entropy.
Finding 1: Answer-time entropy () is unstable as a standalone monitor. Panel (a) of Figure 2 shows that significant conventional discrimination (incorrect answers with lower entropy) appears only in GLM HallusionBench. Most other cells are either directionally inverted or statistically weak, indicating that answer-time entropy depends strongly on model family and dataset and is not a universal uncertainty signal.
Finding 2: Full-generation entropy () is the stronger and more consistent text-only baseline. Panel (b) of Figure 2 shows uniformly negative effect sizes across all nine cells (incorrect answers have higher entropy), with all nine significant.222Exact effect sizes and -values for all nine cells are reported in Appendix Table 8. Effect sizes are larger than answer-time entropy in 8/9 cells, suggesting that aggregating over the full reasoning trace captures uncertainty dynamics that answer-time snapshots miss.
5.3 Architecture-Dependent Grounding Layers
Visual grounding is not uniformly distributed across transformer layers: averaging attention over all layers would dilute the signal from layers that actually encode evidence localization with noise from layers that do not. Before measuring grounding trajectories, we must identify which layers to measure.
Experiment. We compute per-layer AUROC for evidence localization using the procedure defined in §4.3: for each layer, we measure how well head-averaged attention weights discriminate bounding-box tokens from non-evidence visual tokens on the held-out calibration set (100 Visual-CoT samples, independent of evaluation datasets). We evaluate all layers for each of the three models.
Results. Figure 3 shows that grounding layer location differs sharply across architectures. Qwen-2B and Qwen-8B peak in layers 0–5 (AUROC –), with weak retention in later layers. GLM peaks in layers 15–20. The pattern is consistent across datasets.
Layer selection. We set for Qwen-2B and Qwen-8B, and for GLM, consistent with where visual features enter each architecture’s transformer stack [bai2025qwen3vl, meng2024deepstack]. All subsequent attention metrics are computed over these architecture-specific layers.
5.4 Evidence Collapse and Error Prediction
Sampling schedule and canonical positions. To characterize grounding dynamics, we sample attention at seven positions spanning the full generation. Five fall within the reasoning trace: Start (first reasoning token), Early (40% progress), Mid (60%), Late (80%), and EndThink (final token before </think>). Two fall within the answer: AnsStart (first answer token) and AnsEnd (last answer token). Formally, let denote the first reasoning token and the last token before </think>, with . The five reasoning sampling points are at:
Extraction procedure. At each sampled position, we perform a forward pass and compute and (§4.4) over , producing seven-point trajectories per sample. The bounding-box subset is determined by mapping visual tokens to a spatial merge grid and selecting those overlapping the annotated evidence region.
Trajectory metrics. Endpoint metrics capture the residual grounding state at answer time, but two models can arrive at the same via very different paths: one sustaining engagement throughout reasoning, the other collapsing early. To capture the grounding process, we aggregate the five thinking-phase sampling points into trajectory-level features. Treating the five reasoning positions as evenly-spaced ordinal positions , the trapezoidal area under the -trajectory during thinking is:
Higher indicates sustained visual engagement across the reasoning span; lower values indicate early or progressive visual disengagement. We also compute a bbox-weighted variant by substituting for : measures cumulative evidence-region attention rather than total visual attention. The -based metric requires no annotations; the -based counterpart requires bounding boxes.
Finding 3: Universal visual evidence decay across nine cells. Across all nine modeldataset cells, both and decline from Start to AnsEnd. Defining total visual-attention decay as and evidence decay as , per-sample decay is near-universal: for 94.3%–100% of samples per cell, and for 83.5%–100%.333Complete endpoint statistics for both and , including per-sample positive-decay fractions, are reported in Appendix C.
Finding 4: Decay is evidence-specific, not mere attention redistribution. Evidence attention , restricted to visual tokens overlapping the annotated bounding box, also decays in all nine cells (i.e., ), with relative loss ranging from 53.3% (Qwen3-VL-2B HallusionBench) to 90.8% (GLM HallusionBench). This rules out a benign explanation in which visual attention decay is harmless redistribution: absolute evidence mass on relevant regions still declines across cells, indicating disengagement from visual evidence rather than stable evidence-preserving reallocation.
Finding 5: The grounding–correctness relationship follows dataset-dependent trajectories. We track at each of the seven canonical positions and compute Cohen’s per position across modeldataset cells. Figure 4 shows representative trajectories.
Three qualitative patterns recur. Sustained grounding deficit (e.g., HallusionBench, and GLM on MMMU_Pro): incorrect generations are less grounded than correct ones throughout reasoning and into the answer. Crossover (MathVista): incorrect generations begin more visually engaged, then lose grounding mid-reasoning and fall below correct, which can cancel aggregate trajectory summaries such as . Moderate effects (Qwen on MMMU_Pro): trajectory separations are smaller and less consistent, with substantial overlap between correct and incorrect curves.
Key takeaway: the discriminative grounding signal typically emerges (and often peaks) during the reasoning phase rather than at the answer endpoint, so endpoint-only measurement can underestimate or misstate the pattern.444See Appendix D for effect sizes and significance tests.
5.5 Task-Conditional Multimodal Monitoring
We ask whether visual grounding adds monitoring value beyond text-only entropy, and when it should be trusted. Under cross-dataset transfer, we evaluate a probe ladder and an entropy–vision interaction model.
Probe ladder. To test whether vision features improve error prediction, we define L2-regularized logistic regression probes that map features to . We compare nested feature sets of increasing richness: (a) entropy-only: full-generation entropy () or answer-time entropy () alone; (b) entropy + vision: adding answer-time visual attention () and cumulative visual engagement (); (c) full: all features plus generation length () as a control. Features are standardized using training-set statistics only, with per-split regularization strength tuned via grid search. Probe coefficients reveal which signals matter and in which direction.
Transfer protocol. A monitor is only useful if it generalizes beyond its training distribution. We evaluate under cross-dataset transfer: train on dataset , evaluate on held-out . Three datasets with two directions per pair yield six transfers per model and 18 transfer pairs across three models.
Interaction model. The probe ladder tests whether vision features help linearly, but the conceptually important question is what happens when entropy says “confident” (low ) and vision says “disengaged” (low ) simultaneously. We therefore fit a logistic regression with coefficients for an intercept, entropy main effect, vision main effect, entropy-vision interaction, and length control:
where , , are z-scored full-generation entropy, cumulative visual engagement, and generation length respectively. In the confident-but-blind region (, ), the product , so implies a confident-but-blind penalty while implies this regime is comparatively benign.
Selective prediction. Each probe assigns per-example risk ; we defer the riskiest examples to meet a target coverage . We report AUC, coverage-matched AURC, selective risk at fixed coverage, and selective ECE [naeini2015obtaining], averaged ( s.d.) over repeats.
Validation policy (vision veto). To test whether vision can improve entropy-only monitoring as a targeted override rather than linear fusion, we define a two-stage policy (results in Finding 8 below): (1) entropy-only deferral at target coverage; (2) among accepted samples, veto the bottom 5% by cumulative visual engagement (5% chosen as the highest rate that still allows reliable restoration to 90% coverage across transfers); (3) re-loosen entropy threshold to restore target coverage.
Finding 6: Full-generation entropy is strong; naive fusion fails. We compare an entropy-only probe () against a naive entropy+vision probe that adds visual grounding features. Under cross-dataset transfer, full-generation entropy is the strongest baseline and consistently outperforms answer-time entropy, while naive fusion does not reliably improve AUC/AURC and often hurts.555Transfer-level improvement counts and per-model mean AUC deltas are reported in Appendix E.
Entropy is therefore the default monitor, but it has a structural blind spot: it cannot detect visual disengagement.
Finding 7: The confident-but-blind penalty is task-conditional. We fit the interaction model (Eq. 8) per model test dataset cell on deduplicated samples. Figure 5 summarizes the interaction coefficients across all nine cells.
MMMU_Pro shows a statistically significant confident-but-blind penalty. GLM: ; Qwen-8B: (both significant).666Bootstrap 95% CIs are reported in Appendix F.
MathVista shows no such penalty. Interactions are near zero or weakly positive (e.g., GLM: , non-significant), consistent with tasks where symbolic processing correctly replaces sustained visual reference.
The sign flip between MMMU_Pro and MathVista is the key finding. This formalizes why linear fusion fails: a single coefficient cannot accommodate task types where the visionerror relationship has opposite signs. HallusionBench shows weaker and mixed interaction effects due to sparse confident-but-blind samples.777Additional transfer-level probe-coefficient and length-confound analyses are reported in Appendix H.
For interpretability, we define quadrants using a binary split at on entropy (confident vs. uncertain) and visual engagement (grounded vs. blind), a design choice balancing tail focus with usable per-quadrant sample counts: Q1 = confident + grounded, Q2 = confident + blind, Q3 = uncertain + grounded, Q4 = uncertain + blind. Quadrant analysis corroborates this contrast: the confident+blind region is systematically riskier than confident+grounded on MMMU_Pro, but comparatively safe on MathVista.888Exact Q2–Q1 gaps, split-consistency counts, and per-quadrant sample sizes are reported in Appendix G. Figure 6 visualizes this contrast as a continuous regime map for GLM.
Finding 8: Final validation of the task-conditional structure. At 90% coverage (5% veto rate), the veto helps where the interaction predicts danger and hurts where it predicts safety (Table 1). Overall, it improves 6/18 transfers and hurts 12/18; the direction is perfectly predicted by the sign of , validating the task-conditional structure.
6 Discussion and Conclusion
Discussion. Extended reasoning can create the same failure mode it is intended to reduce: as generated text grows, text priors strengthen while visual grounding decays. Visual disengagement is not uniformly pathological; it signals danger on tasks requiring sustained visual reference and can be appropriate on tasks that transition to symbolic processing after early visual extraction. This implies that task characterization must precede monitor design, because the same vision signal can indicate opposite risk regimes across tasks. The resulting deployment principle is simple: use full-response entropy as the default monitor and add vision-based checks conditionally when sustained visual grounding is required.
Limitations. (1) Attention is correlational in this study: we measure attention mass, not causal influence. Establishing causality requires intervention tests such as activation patching. (2) Model scale is limited to 2B–8B parameters. Generalization to larger RVLMs remains unverified. (3) Architecture coverage is limited to two model families. Broader architectural diversity may reveal additional grounding dynamics. (4) Data size is 300 examples per dataset, and normal-stop filtering reduces effective sample size in some cells. This limits precision for cell-level inference. (5) Extreme-corner analysis at uses small per-cell samples (typically –), limiting cell-level statistical power. Our primary evidence for task-conditional risk is the continuous interaction model in Eq. 8, fit over the full sample; quadrant error rates are included only for interpretability and visualization. (6) Task type is treated as known at analysis time. Zero-shot deployment requires reliable automatic classification of whether sustained visual grounding is needed.
Future Work. Next steps should move from correlation to intervention by testing whether preserving visual grounding causally improves correctness via activation patching. We also need scale and coverage: 72B+ RVLMs and additional architectures. Practical deployment requires automatic task-type inference that triggers conditional vision monitoring without manual routing.
Conclusion. We established four findings: evidence collapse is a general property of RVLM generation, full-response entropy is the strongest universal monitor but remains blind to visual disengagement, the error implications of disengagement are task-conditional, and monitoring design must therefore be task-aware rather than globally fused. The deployment question is not whether to add vision, but whether the task requires sustained visual grounding.
References
Appendix
Appendix A Inference Setup, Prompt Templates, and Answer Parsing
A.1 Inference Infrastructure
Response generation uses vLLM [kwon2023efficientmemorymanagementlarge] with FlashAttention. Attention extraction is performed separately: each model is loaded with HuggingFace Transformers (attn_implementation=eager) and served as a local FastAPI endpoint on a single NVIDIA RTX 3090 (24 GB). For each response, we perform seven targeted forward passes at the canonical probe positions defined in §5.4 to extract head-averaged attention over . Generation hyperparameters are reported in Table 2 (Appendix A.1).
A.2 Prompt Templates
Each dataset uses a fixed prompt template consisting of a hint (instructing the desired answer format), the question, and, for multiple-choice items, a choices block. The templates are shown below.
A.3 Output Segmentation and Termination
Each assistant response is normalized into full_text, reasoning_text, and answer_text using three parser modes: inline_think_tags (explicit <think>...</think> in content), separate_reasoning_field (reasoning returned in a separate message field), and content_only (no explicit reasoning field). For separate_reasoning_field, full_text is the concatenation of reasoning and answer; otherwise it is the raw content text.
Termination classification.
We classify each generation into one of three termination types, applied in priority order: The loop detector uses two complementary repetition measures. The unique 4-gram ratio is the number of distinct token 4-grams divided by the total number of 4-grams in a span; a ratio of 1.0 indicates no repeated 4-gram, while values near 0 indicate near-verbatim cycling. The zlib compression ratio is the length of the zlib-compressed byte string divided by the uncompressed length; highly repetitive text compresses to a small fraction of its original size, yielding ratios near 0. We require both heuristics to fire jointly to avoid false positives from legitimately structured (but non-looping) reasoning.
-
1.
max_tokens_loop: The generation reached max_tokens and exhibits repetition, detected when both the unique 4-gram ratio on the trailing 25% of tokens falls below 0.5 and the zlib compression ratio of the full text falls below 0.15.
-
2.
max_tokens: The generation reached max_tokens without triggering the loop heuristic above.
-
3.
normal_stop: The generation terminated via an EOS token or a successful </think>–to–answer transition.
We mark think_end_found if any of the following is true: a token-level </think> position is found, the parsed message structure indicates reasoning has ended, or </think> appears in full_text. If termination is max_tokens or max_tokens_loop and think_end_found is false, evaluation text is forced to empty. Otherwise, evaluation text is selected as: answer_text for separate_reasoning_field, or answer_text when non-empty (fallback to full_text) for the other parser modes.
A.4 Answer Extraction and Correctness Matching
All datasets use the same extraction-and-matching procedure on the selected evaluation text.
No-choice path (e.g., MathVista numeric/free-form).
When choices is empty, the matcher first extracts the last integer from the last non-empty line; if absent, it searches the last 300 characters. If both prediction and ground truth yield integers, correctness is exact integer equality. If integer extraction fails, it falls back to normalized string equality.
Choice path (MathVista MCQ, HallusionBench, MMMU_Pro).
When choices is non-empty, matching proceeds in order:
-
1.
Extract the last option letter (A--D) from the prediction tail (last 300 characters, then full text as fallback). If found, compare by option index (or directly to letter-form ground truth when applicable).
-
2.
If no letter is found, choose the option text whose normalized form appears latest in the normalized prediction text.
-
3.
Final fallback: normalized full-string equality against ground truth.
For HallusionBench, this same choice path is applied with choices = [Yes, No].
Appendix B Full Entropy Discrimination Results
This appendix reports the full cell-wise entropy statistics underlying §5.2 and Figure 2, using the same filtering and effect-size definitions as the main text.
B.1 Termination Type Breakdown
Table 6 reports raw termination outcomes before filtering.
B.2 Sample Counts After Normal-Stop Filtering
Table 7 shows the effective sample sizes used in all downstream analyses (not just entropy).
B.3 Full-Response Entropy Discrimination
B.4 Answer-Span Entropy Discrimination
Table 9 reports answer-span entropy for comparison; several cells are non-significant despite consistent inversion in most Qwen cells.
Appendix C Complete Evidence Decay Results
This appendix provides the full endpoint-decay table referenced in §5.4 (Finding 3 and Finding 4), including all nine modeldataset cells and per-sample decay-positive counts.
C.1 Universal Evidence Decay Tables
Tables 10 and 11 report the full endpoint statistics underlying §5.4, Findings 3 and 4. For each modeldataset cell we record visual attention mass at the first reasoning token and at the last answer token:
with as defined in Eq. 4. Total visual attention decay is ; evidence attention decay is defined analogously using (Eq. 5). Both quantities are positive when attention decays over the course of generation.
Table 10 reports total visual attention decay. in all nine cells, with per-sample decay-positive fractions ranging from 94.3% to 100%. Table 11 restricts to evidence-region tokens overlapping the annotated bounding box. in all nine cells, with relative loss ranging from 56.8% (Qwen3-VL-2B HallusionBench) to 90.8% (GLM HallusionBench).
C.2 RVAR Decay Patterns
Across both Qwen models, mean is negative in every dataset (§5.4), indicating a “less but smarter” pattern: residual visual attention becomes relatively more concentrated on relevant regions even as total visual attention drops. Here, the relevant visual attention ratio is defined as
i.e. the fraction of total visual attention mass allocated to evidence-region tokens at generation step , and . For GLM-4.6V-Flash, is mixed and near zero overall (MathVista: , HallusionBench: , MMMU_Pro: ), so directional interpretation is inconclusive.
This is a secondary observation. The primary quantity in the main text (Finding 3 and Finding 4) is absolute evidence mass, , which decays across all nine cells (Table 11) regardless of direction.
Appendix D Grounding Trajectory Effect Sizes
This appendix reports the quantitative trajectory discrimination statistics referenced in §5.4 (Finding 5). We compute Cohen’s (incorrect vs. correct) at each canonical position and use Welch two-sample t-tests for significance.
At AnsEnd, Cohen’s is negative in 9/9 modeldataset cells and significant in 7/9. Peak discrimination occurs during the reasoning phase (Start–EndThink) in 7/9 cells. Endpoint-only measurement can therefore miss substantial signal; the maximum loss is 77.5% (GLM MMMU_Pro).
The three qualitative trajectory patterns described in §5.4 correspond to the following cell-wise statistics:
-
•
Sustained deficit (HallusionBench all models; GLM MMMU_Pro; 4 cells): Cohen’s ranges from to , all .
-
•
Crossover (MathVista all models; 3 cells): at Start, to ; is non-significant () due to sign cancellation across the trajectory.
-
•
Moderate (Qwen MMMU_Pro; 2 cells): Qwen-8B is significant (, ), while Qwen-2B is underpowered (, , ).
In aggregate, is significant in 5/9 cells; all three MathVista null results are explained by crossover cancellation.
Appendix E Naive Fusion Transfer Summary
This appendix reports the summary statistics underlying §5.5, Finding 6. Table 4 lists all 18 transfer pairs with exact AUC values for entropy-only and entropy+vision probes. Across these 18 pairs, the entropy-plus-vision probe improves AUC in only 4/18 transfers and improves coverage-matched AURC in only 4/18. Mean AUC deltas (visionentropy) are: GLM , Qwen-2B , and Qwen-8B . Full-generation entropy outperforms answer-time entropy in 16/18 transfer pairs.
Appendix F Interaction Coefficient Confidence Intervals
Appendix G Quadrant Analysis Details
This appendix reports the quadrant summary referenced in §5.5, Finding 7 (Table 5). Quadrants are defined by splitting entropy (confident vs. uncertain) and visual engagement (grounded vs. blind) at (Q1–Q4 as in the main text). For MMMU_Pro targets, Q2 (confident + blind) error rates are 15–40 percentage points above Q1 (confident + grounded) for all three models, with 10/10 split consistency in every transfer. For MathVista targets, Q2 error rate is 0% in all transfers. HallusionBench targets have Q2 sparsity (four transfers with ), so Q2–Q1 gaps are undefined there. Where Q2 is populated, cell sizes remain small (typically –), so the primary signal is directional consistency rather than per-cell significance.
Appendix H Probe Coefficient Analysis and Length Confound
This appendix addresses two methodological questions: what probe coefficients mean, and whether is only a proxy for generation length. We use the same transfer protocol as §5.5: 18 directed transfers (3 models 6 transfers), with stratified splits per transfer (180 total fits).
H.1 Coefficient Sign Consistency Across Transfers
We report sign consistency as the fraction of fits where a coefficient keeps the same direction. This isolates directional reliability under transfer, independent of absolute magnitude.
Table 12 reports raw sign/non-zero counts only. In addition to the features defined in §4–5.4, the probe ladder includes a reengagement feature measuring the recovery of visual attention between the end of reasoning and the answer phase:
where and are evaluated at the first answer token and final reasoning token, respectively. Positive values indicate the model re-attends to visual tokens when transitioning to the answer.
The main interpretation is: is the most direction-stable trajectory feature; is less stable for GLM than for Qwen; exhibits model-family direction inversion; is uniformly negative in the length-only probe; is effectively dead for Qwen (all-zero coefficients); is directionally unstable; and is stable for GLM but flips sign for Qwen by training dataset.
H.2 Length Confound Analysis
This analysis tests a length confound: visual attention may mechanically decay with longer generations, making redundant with . The falsification criterion is that, if were only length, its incremental effect would disappear once length is included. Table 13 reports only core quantities: are Pearson correlations between and ; is the number of transfers where has larger NLL permutation importance than in the joint probe; and is the number of splits with positive coefficient in that same joint probe. Interpretation: the correlations are moderately negative rather than near-deterministic, indicating coupling without redundancy. In the joint two-feature probe, dominates in 10/18 transfers, and its coefficient remains positive in 148/180 splits under explicit length control. If were only a length proxy, both results would collapse after conditioning on .
These results are inconsistent with a “length-only” explanation. Length and visual engagement are coupled, but not interchangeable, and retains independent predictive semantics under direct length control.

KAYNAK : https://arxiv.org/abs/2604.04207